

let str = '      tech     global    ';
console.log(str.trim());  // tech   global
console.log(str.trimEnd());
console.log(str.trimStart());

/*   trim()
  TASK::removes the whote spaces at the beginning and the end of a string,
 RETURN:retuens a new modified string, white spaces removed
 ARGUMENT: no args
 CHANGE ORIGINAL VALUE: does not modify the original value
  STATIC or NON-STATIC ; non-static (if calling it with variable, is non static)
*/















