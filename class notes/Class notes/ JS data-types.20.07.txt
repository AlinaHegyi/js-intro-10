
  Data types

Primitive types: basic types that usually store a piece of information
  string; text
  number: number
  bigInt: large number
  booLean: true or false
  undefined:
  null:
  symbol;


Reference types : complex types which can store more information
   -arrays, objects, maps, sets, dates, math



1.String ;a sequence of characters (zero,one or multiple characters)
  We store strings in single or double quotes
    ''        - empty strings
    ' '       -strings
  '123'      -strings
   'undefined' -strings
    'null'  -strings


2 Number:is usedt to store all absolute and decimal numbers (8 byte)
 Whole numbers -absolute numbers 1, 2, 18, 25, 100,
 Decimal numbers- floating numbers : 2.5 , 3.6 , 10.99


 3 BigInt : used to store larger numbers (more than 8 byte)




  4 boolean

 = vs == vs ===
  = assingment operator                3 = 3   -true
  == comparison operator (loose)       3 == 3    -true (it checks only the value, not the data type)
  ===comparison operator (strict)      3 === 3   -false (it checks both the value and data types) 




Deprecated-depracation :was an old reserved variable name
  var is reserved keyword but might get deprecated in the future as we use let and consy

 undefined vs null
     undefined is the default type when you declare a variable but not provide any value
     null is specifically stating that this variable holds no value. Absence of information

 typeof undefined will return undefined
 typeof null will return object


OBJECTS data type is used to store key-value pairs, properties

 Object data type is used to store key-value pairs, properties.
firstName => 'John'
lastName => 'Doe'
age => 25
phoneNumber => '000-000-0000'
email => 'johndoe@gmail.com'
password => 'john1234'










